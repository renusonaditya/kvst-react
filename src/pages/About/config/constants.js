const SCROLL_ID = "scroll-id";

export const scrollIds = Object.freeze({
  aboutTemple: `about-temple__${SCROLL_ID}`,
  history: `history__${SCROLL_ID}`,
  generalInformation: `general-information__${SCROLL_ID}`,
});
